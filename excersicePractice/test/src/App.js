import { useState } from 'react'
import Button from './Components/Button'

const App = () => {
/* 
  const orders = [
    {amount: 250},
    {amount: 400},
    {amount: 100},
    {amount: 325}
  ]
  const totalAmount = orders.reduce((sum, order) => { return sum + order.amount}, 0)
  const totalAmount = orders.reduce((sum, order) => sum + order.amount, 0) 
*/

return(
<p>hello puta</p> 
)
}

export default App